<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Login - Shortly</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #1e1e1e;
            color: #f2f2f2;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar {
            background-color: #111;
            padding: 15px 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        .nav-link {
            text-decoration: none;
            color: #00bcd4;
            font-weight: bold;
            font-size: 18px;
            background-color: #222;
            padding: 8px 12px;
            border-radius: 6px;
            transition: background-color 0.3s;
        }

        .nav-link:hover {
            background-color: #00bcd4;
            color: #fff;
        }

        .main {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 50px;
            min-height: 80vh;
        }

        .form-box {
            background-color: #2c2c2c;
            padding: 40px;
            border-radius: 10px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 0 12px rgba(0, 0, 0, 0.6);
        }

        .form-box h2 {
            color: #00bcd4;
            text-align: center;
            margin-bottom: 25px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 6px;
            font-weight: bold;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 6px;
            background-color: #444;
            color: #f2f2f2;
            font-size: 16px;
        }

        .form-group input:focus {
            outline: none;
            background-color: #555;
        }

        .submit-btn {
            width: 100%;
            padding: 12px;
            background-color: #00bcd4;
            color: #fff;
            font-weight: bold;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .submit-btn:hover {
            background-color: #009eb3;
        }

        .link {
            color: white;
        }

        #warning {
            color: #16e144;
            font-weight: bold;
        }
    </style>
</head>

<body>

    <div class="navbar">
        <a class="nav-link" href="/">🏠 Home</a>
    </div>

    <div class="main">
        <div class="form-box">
            <h2>Login</h2>
            <form action="/login" method="POST">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required>
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>

                <button class="submit-btn" type="submit">Login</button>
            </form> <br>
            <% if(warning){ %>
                <span id="warning">Invalid Id/Password</span> <br><br>
                <% } %>
                    <a href="/user/forgot-password" class="link">Forgot Password??</a> <br>
                    <a href="/register" class="link">Register?</a>
                    <form action="/login" method="POST">
            <input type="hidden" name="email" value="studypurpose78588@gmail.com">
            <input type="hidden" name="password" value="1234">
            <button type="submit">Login as Guest</button>
        </form>
        </div>
        

    </div>

</body>

</html>