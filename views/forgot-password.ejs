<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            min-height: 100vh;
            margin: 0;
            padding-top: 100px;
            display: flex;
            flex-direction: column;
            align-items: center;
            color: #fff;
        }

        nav {
            position: absolute;
            top: 20px;
            right: 30px;
        }

        nav a {
            margin-left: 20px;
            text-decoration: none;
            color: #ffffff;
            font-weight: bold;
            font-size: 18px;
            transition: color 0.3s;
        }

        nav a:hover {
            color: #f0db4f;
        }

        .card {
            background: #ffffff;
            color: #333;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            width: 360px;
            text-align: center;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .card h2 {
            margin-bottom: 10px;
            font-size: 22px;
            color: #2a5298;
        }

        .card input[type="email"] {
            padding: 12px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 6px;
            transition: border-color 0.3s;
        }

        .card input[type="email"]:focus {
            outline: none;
            border-color: #2a5298;
        }

        .card button {
            background-color: #116e26;
            color: white;
            padding: 12px;
            font-size: 16px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .card button:hover {
            background-color: #218838;
        }
        #warning{
            color: red;
            font-weight: bold;
        }
        .details{
            text-align: left;
        }
    </style>
</head>

<body>

    <nav>
        <a href="/">Home</a>
        <a href="/track">Track</a>
        <a href="/about">About Us</a>
        <a href="/login">🔐Login</a>

    </nav>

    <div class="card">
        <h2>Forgot Password</h2>
        <p class="details">Enter your email address and we'll send you a reset link.</p>
        <form action="/user/forgot-password" method="POST">
            <input type="email" name="email" placeholder="Your email address" required>
            <button type="submit">Submit</button>
        </form>
        <% if(warning){ %>
            <span id="warning">Account does not exists.</span>
            <a href="/register">Register?</a>
        <% } %>
    </div>

</body>

</html>