<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Profile</title>

    <style>
        /* ─── Base layout ─────────────────────────────────────────────── */
        body {
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            margin: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 16px;
        }

        /* ─── Card container ──────────────────────────────────────────── */
        .card {
            width: 100%;
            max-width: 420px;
            background: #fff;
            padding: 36px 44px;
            border-radius: 16px;
            box-shadow: 0 12px 28px rgba(0, 0, 0, .25);
            animation: fadeIn .6s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(12px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ─── Headings ────────────────────────────────────────────────── */
        h1 {
            margin: 0 0 28px;
            font-size: 1.6rem;
            text-align: center;
            color: #1e3c72;
        }

        /* ─── Form elements ───────────────────────────────────────────── */
        label {
            font-weight: 600;
            margin-bottom: 6px;
            display: block;
            color: #333;
        }

        input[type=text],
        input[type=email],
        input[type=number] {
            width: 100%;
            padding: 12px 14px;
            margin-bottom: 22px;
            border: 1px solid #ccd1e0;
            border-radius: 8px;
            font-size: 15px;
            transition: border-color .25s;
        }

        input:focus {
            outline: none;
            border-color: #0072ff;
            box-shadow: 0 0 0 3px rgba(0, 114, 255, .15);
        }

        /* ─── Primary button ─────────────────────────────────────────── */
        button {
            width: 100%;
            padding: 13px;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            background: #0072ff;
            color: #fff;
            transition: background .25s, transform .1s;
        }

        button:hover {
            background: #0062e0;
        }

        button:active {
            transform: scale(.98);
        }

        /* ─── Back link ──────────────────────────────────────────────── */
        .back-link {
            display: block;
            margin-top: 20px;
            text-align: center;
            text-decoration: none;
            color: #0072ff;
            font-weight: 600;
            transition: color .25s;
        }

        .back-link:hover {
            color: #0059d6;
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>Edit Profile</h1>

        <form action="/profile/edit/save" method="POST">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" value="<%= user.name %>" placeholder="Your full name" required>

            <label for="age">Age</label>
            <input type="number" id="age" name="age" value="<%= user.age %>" min="1" max="120" required>

            <button type="submit">Save Changes</button>
        </form>

        <a href="/profile" class="back-link">← Back to Profile</a>
    </div>
</body>

</html>